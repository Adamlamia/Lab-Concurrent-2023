class Update {
    static int statValue = 0;
    int instValue = 0;
    synchronized void updateSum(int) {
    Thread tThread.currentThread( for (int n = 1; n <= 5000; n++) {
    atetValue + Le
    System.out.println(getName()+", Iteration " + n + ": atatXalue=" + atatXalue + ";
    inetValue="
    }
    + instValue);
    }
    class Aclass extends Thread {
    Update u= new Update();
    public void run() {
    y.updateSum(10);
    class Test {
    public static void main(String[] args) {
    Aclass one new Aclass);
    Aclare two = new Actaga)
    Thread t1 = new Thread (one):
    Thread t2= new Thread (one)
    Thread t3= new Thread (two)
    tl.setName("Thread 1");
    t2.setName("Thread 2");
    t3.setName("Thread 3");
    tl.start();
    t2.start();
    t3.start();
    }
    }